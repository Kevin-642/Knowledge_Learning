{% extends 'base.html.twig' %}

{% block title %}Réinitialisation de mot de passe{% endblock %}

{% block body %}
    <div class="container my-5">
        <div class="alert alert-info" role="alert">
            <h4 class="alert-heading">Si un compte correspondant à votre adresse e-mail existe</h4>
            <p>Un e-mail contenant un lien pour réinitialiser votre mot de passe vient d'être envoyé. Ce lien expirera dans 
            {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>
            <hr>
            <p class="mb-0">Si vous ne recevez pas d'e-mail, veuillez vérifier votre dossier spam ou <a href="{{ path('app_forgot_password_request') }}">réessayer</a>.</p>
        </div>
    </div>
{% endblock %}
